import Logger from '@ptkdev/logger';
import { Client, Message } from 'discord.js-selfbot-v13';
export declare const handleMessage: (logger: Logger, client: Client, message: Message) => Promise<void>;
